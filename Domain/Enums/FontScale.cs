namespace Domain.Enums;

/// <summary>
/// Mức phóng to chữ cho người lớn tuổi.
/// UI sẽ map enum này sang CSS scale / font-size.
/// </summary>
public enum FontScale
{
    Medium = 1,  // M
    Large = 2,   // L
    XLarge = 3   // XL
}
